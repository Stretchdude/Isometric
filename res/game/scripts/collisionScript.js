;importPackage(Packages.org.newdawn.slick.geom);

if(!entity2.getName().toString().equals("movingEntity")
  && entity1.getName().toString().equals("player")) {
  
	//vec = entity1.position;
	//newVec = new Vector2f(vec.x + 30, vec.y + 30);
	//entity1.position = newVec;
	map.remove(entity2);
}